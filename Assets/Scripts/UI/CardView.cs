// CardView.cs — stub vide, plus utilisé en mode IMGUI
// Conservé pour éviter les erreurs de référence dans l'Inspector
using UnityEngine;

namespace BeloteFreeze.UI
{
    public class CardView : MonoBehaviour
    {
        public void SetCard(BeloteFreeze.Core.Card card, BeloteFreeze.Core.Suit trump) { }
        public void SetPlayable(bool playable) { }
    }
}
